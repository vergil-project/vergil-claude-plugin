
# Release 2.1.6 (2026-06-09)

## Documentation

- **add dual-agent audit test kit design (vergil-tooling#1563) (#468)**
Runbook+fixtures design for the issue-implement/issue-audit dual-agent loop, relocated here so the audit (which shares the vergil-tooling fixture worktree) never sees the test plan. Single-branch six-flaw chain, golden at end; marquee goal is a repeatable manual methodology to verify the feature. Depends on vergil-tooling#1564 (revise PR metadata) and #1566 (vrg-reword).

- **add dual-agent audit test runbook (vergil-tooling#1563) (#470)**
The USER agent's prescriptive script for the six-flaw chain, with verified-green golden fixtures and the coverage-complete weak test. Lives here so the audit (which shares the vergil-tooling fixture worktree) never sees it. Literal round text to be tuned against live audit runs.


## Features

- **sequential handshake: issue-audit takes a worktree path; transparent loops (vergil-tooling#1572) (#469)**
issue-implement now implements directly then engages the oracle only at the ready/handoff point (creating the worktree, emitting the /issue-audit <worktree-path> hand-off), so the audit starts when there is something to review. issue-audit takes the worktree path, cd's in, and runs with no issue number. Both skills mandate foreground, narrated, chatty execution — the visible back-and-forth is the human oversight.
