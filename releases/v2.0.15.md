
# Release 2.0.15 (2026-05-28)

## Chores

- **revert container-prefix dev workaround**
Remove container-prefix: dev overrides from ci.yml and cd.yml now that prod container images are published to GHCR. Jobs will use the default prod prefix. Ref #410
