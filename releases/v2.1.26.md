
# Release 2.1.26 (2026-07-04)

## Features

- **route intake to .github with --kind (triage-capture, memory-audit) (#575) (#581)**
Epic vergil-project/.github#85, Task 8. Update the intake-creating skills to the new model: triage-capture drops the most-relevant-repo routing and always files in the org .github, choosing --kind triage|idea|research (was: triage label + per-repo choice). memory-audit's plugin/skill-issue path uses --kind triage into .github (the tool's default). Keeps every non-.github repo to single-PR tasks only; the org-wide intake queue is one filtered view.
